{
  "destinationUrl": "http://localhost:3001",
  "method": "POST",
  "request": {
    "transform": "ToXml",
    "adds": {
      "header": {
        "tes": "ololol",
        "tes2": "$body[user][name][0]"
      },
      "body": {
        "user.address": "$header[Content-Type][0]",
        "user.age": "tes1",
        "user.query": "$query[tes][0]"
      },
      "query": {
        "tesquery": "$header[Content-Type][0]"
      }
    },
    "deletes": {
      "header": [
      ],
      "body": [
      ],
      "query": [
      ]
    },
    "modifies": {
      "header": {
      },
      "body": {
        "user.lol": "user lol",
        "user.name": "$body[user][cars][0]"
      },
      "query": {
      }
    }
  },
  "response": {
    "transform": "ToJson",
    "adds": {
      "header": {
        "tesheaderresponse": "tesheaderresponseffuuu"
      },
      "body": {
        "user.name": "$header[Content-Type][0]",
        "user.response": "$header[tesheaderresponse]"
      },
      "query": {
      }
    },
    "deletes": {
      "body": [
      ]
    },
    "modifies": {
      "body": {
      }
    }
  }
}
